const blue = "#5045CD";
const sky = "#A8B6FF";
const babyBlue = "#F5F8FA";
const white = "#fff";

const offsetTop = "165px";

const headline = "25px";
const headlineSmall = "21px";
const bodyTextLarge = "18px";
const bodyText = "16px";

const spacing = 8;
const gap = `${spacing}px`;
const size = `${spacing * 4}px`;
const sizeHalf = `${(spacing * 4) / 2}px`;
const sizeLarge = `${spacing * 4 * 2}px`;

export const theme = {
  blue,
  sky,
  babyBlue,
  white,

  offsetTop,

  headline,
  headlineSmall,
  bodyTextLarge,
  bodyText,

  gap,
  size,
  sizeHalf,
  sizeLarge,
};
